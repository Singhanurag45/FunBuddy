# Activate with: --spring.profiles.active=dev (do not use in production).
# Postman: append ?trace=true to the URL to include "trace" (stack) in JSON error responses.
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on_param
